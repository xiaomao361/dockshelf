# 搁这儿：项目启动文档

> 暂时要用的文件，先搁这儿。

## 1. 当前决定

“搁这儿”是一个面向 macOS 和 Windows 的轻量文件暂存工具，不再以 Deepin 插件开发赛为目标，也不承担 Deepin SDK 兼容。

首选技术栈为 **Flutter + Dart**。但该选择必须先通过跨应用文件拖放探针；探针失败时停止正式开发，重新评估 Qt 或原生实现，不为了保留已写 UI 而硬撑技术栈。

开发顺序：

1. macOS 技术探针；
2. macOS MVP 与真实自用；
3. Windows 技术验收与适配；
4. 再决定签名、分发、自动更新和商业化。

### 当前暂停状态（2026-08-04）

项目在安装 Flutter SDK 和创建代码工程之前暂停，先解决开发机磁盘容量问题。

暂停时已确认：

- macOS Data 卷仅剩约 11 GiB 可用空间，使用率约 95%；
- 本机已有 Xcode 26.6，但当前终端环境中没有可用的 Flutter / Dart；
- Flutter SDK 已包含 Dart，不单独安装 Dart 编译器；
- 当前阶段只需要 macOS 工具链，不安装 Android Studio，也不在这台 Mac 上安装 Windows 编译环境；
- 尚未安装依赖、创建 Flutter 工程或运行任何技术探针。

恢复开发前，先在以下方案中完成选择并验证实际空间：

1. 更换或扩容为 1 TB 内置存储，并确认重要数据具有可恢复备份；
2. 使用稳定挂载的外接 SSD，将 Flutter SDK、Pub 缓存和实际编译工作区放在外接盘；
3. 若继续使用当前内置盘，先通过有边界的清理将可用空间恢复到至少 25–30 GiB，再安装工具链。

磁盘方案未确定前，不为了推进项目而安装 Flutter，也不擅自删除 Xcode、项目、聊天、个人文件或其他不可替代数据。

恢复后的第一步仍然是 macOS M0 拖放探针；Windows 工程与工具链等 macOS 探针通过后再处理。

## 2. 产品定义

### 一句话

把暂时要用的文件放进一个随时可以唤出的搁板，稍后再拖到聊天、邮件、浏览器或其他文件夹。

### 核心动作

```text
唤出搁板 → 拖入文件 → 隐藏 → 再次唤出 → 把文件拖走
```

### 与剪贴板管理器的区别

- 剪贴板管理器被动记录复制历史；
- “搁这儿”只接收用户主动拖入的文件；
- 不读取全部剪贴板历史；
- 不复制文件内容，不上传云端；
- 不试图替代文件管理器。

## 3. 产品原则

1. **只帮用户临时放一下。** 不引导用户整理、归档或建立知识库。
2. **文件仍属于原位置。** 搁板只保存引用，不创建隐藏副本。
3. **动作可见。** 拖入、失效、移除和清空都必须有明确反馈。
4. **默认本地。** 不联网、不注册账号、不做遥测。
5. **保持轻量。** 常驻时资源占用、启动速度和安装包大小都是产品质量的一部分。
6. **平台体验优先于代码复用率。** macOS 和 Windows 可以有少量不同交互，但产品语义必须一致。

## 4. Phase 0：技术探针

### 目标

在投入正式 UI 前，证明 Flutter 可以完成“搁这儿”最关键的跨应用拖放闭环。

### 建议依赖

- Flutter / Dart
- `super_drag_and_drop`
- `window_manager`

`tray_manager` 和 `hotkey_manager` 暂不进入第一段探针，避免把问题混在一起。

### 探针界面

只做一个普通窗口：

- 上半部分是文件接收区；
- 下半部分显示收到的文件路径；
- 每个文件显示一个可以向外拖出的简单卡片；
- 不做正式视觉、不做动画、不做持久化。

### macOS 必须验证

1. 从 Finder 拖入一个文件；
2. 从 Finder 一次拖入多个文件；
3. 从探针窗口把文件拖回 Finder；
4. 从探针窗口拖入浏览器文件上传区域；
5. 从探针窗口拖入至少一个真实聊天或邮件应用；
6. 拖放过程中不复制、移动或修改原文件；
7. 文件名包含中文、空格和特殊字符时仍然正确；
8. 文件被删除后，界面能识别引用失效，而不是崩溃。

### Windows 后续必须验证

1. 从资源管理器拖入和拖回；
2. 拖入浏览器、微信/聊天工具和 Office/邮件应用；
3. 中文路径、长路径和空格路径；
4. 不同 DPI 和多显示器下的拖放坐标；
5. 安装包运行环境中行为与开发环境一致。

### 探针通过标准

macOS 的单文件、多文件、向 Finder 拖出和向至少两个外部应用拖出全部成功，且不需要自行维护大段 AppKit 原生拖放代码。

如果核心闭环需要大量 Swift/AppKit 补丁，Flutter 选型暂不成立。

## 5. MVP 范围

技术探针通过后，MVP 只包含以下能力。

### 必须有

- 从菜单栏/系统托盘唤出搁板；
- 支持全局快捷键唤出；
- 接收一个或多个本地文件或文件夹；
- 显示系统图标、名称和失效状态；
- 支持把条目拖到其他应用；
- 支持单项移除；
- 支持一键清空；
- 点击关闭时隐藏到后台，而不是退出；
- 深色和浅色外观可读；
- 默认不联网、不上传、不复制文件。

### 首版限制

- 最多暂存 20 个条目；
- 相同规范路径只保留一项；
- 默认仅保留当前运行会话；
- 应用退出或重启后清空；
- 不承诺跟踪被移动后的文件；
- 原文件消失时保留失效卡片，等待用户移除。

### 明确不做

- 剪贴板历史；
- 文本和图片收藏；
- 云同步或局域网传输；
- 登录和账号系统；
- 搜索、标签、文件分类；
- 文件预览和内容索引；
- 自动复制到应用私有目录；
- 拖动任意文件时全局检测并自动弹出；
- 启动时恢复上次搁板；
- 自动更新；
- Linux、移动端或 Web 版本。

## 6. 唤出方式

MVP 使用两条可靠路径：

1. 菜单栏/系统托盘图标；
2. 用户可设置的全局快捷键。

“用户开始在系统任意位置拖文件时，搁板自动出现”暂不实现。该能力需要更深入的 AppKit/Win32 全局事件处理，应在 MVP 证明产品价值后单独评估。

## 7. 建议工程结构

```text
dockshelf/
├── lib/
│   ├── app/
│   │   ├── dockshelf_app.dart
│   │   └── app_lifecycle.dart
│   ├── domain/
│   │   ├── shelf_item.dart
│   │   └── shelf_state.dart
│   ├── features/
│   │   └── shelf/
│   │       ├── shelf_controller.dart
│   │       ├── shelf_window.dart
│   │       ├── drop_target.dart
│   │       └── shelf_item_card.dart
│   ├── platform/
│   │   ├── drag_drop_service.dart
│   │   ├── tray_service.dart
│   │   ├── hotkey_service.dart
│   │   └── window_service.dart
│   └── main.dart
├── test/
│   ├── shelf_state_test.dart
│   └── shelf_controller_test.dart
├── integration_test/
├── macos/
├── windows/
├── docs/
│   ├── PRODUCT.md
│   ├── DRAG_DROP_PROBE.md
│   └── ACCEPTANCE.md
└── README.md
```

不要为了“架构完整”提前拆包或建立独立共享库。只有 macOS 与 Windows 真正出现不同实现时，才把差异放进 `platform/`。

## 8. 数据模型

MVP 的 `ShelfItem` 最少包含：

```text
id              本次运行内稳定 ID
path            原文件绝对路径
displayName     展示名称
kind            file / directory
addedAt         拖入时间
exists          当前是否仍然存在
```

模型只描述文件引用，不保存文件内容。`exists` 是读取时派生状态，不应假装原文件仍然可用。

## 9. 交互状态

至少覆盖以下状态：

- 空搁板；
- 文件拖入悬停；
- 成功接收单个文件；
- 成功接收多个文件；
- 重复文件；
- 已失效文件；
- 正在向外拖出；
- 移除单项；
- 清空全部；
- 达到 20 项上限。

空状态文案建议：

> 把待会儿要用的文件拖到这里。

清空后的短反馈建议：

> 搁板空了。

## 10. 验收标准

MVP 只有在以下条件全部满足时才算完成：

1. macOS 与 Windows 都通过真实跨应用拖入、拖出；
2. 原文件没有被复制、移动或修改；
3. 原文件删除后应用不崩溃，并显示失效状态；
4. 菜单栏/托盘和快捷键均能稳定唤出；
5. 关闭窗口后仍能再次唤出；
6. 清空行为只清除引用，不删除磁盘文件；
7. 深浅主题和常见缩放比例可用；
8. 空闲常驻资源、启动时间和安装包大小被实际测量并记录；
9. 至少完成一周真实自用，记录自然使用次数和失败场景；
10. 未经明确决定，不增加 MVP 外功能。

## 11. 首轮实施顺序

### M0：跨应用拖放探针

- 只初始化 macOS Flutter 工程；
- 完成最丑但真实的拖入、拖出；
- 保存验证记录；
- 给出 Flutter 继续或停止结论。

### M1：单窗口搁板

- 内存态条目列表；
- 重复处理；
- 失效检测；
- 移除和清空；
- 基础单元测试。

### M2：桌面工具形态

- 无边框小窗口；
- 菜单栏/托盘；
- 全局快捷键；
- 隐藏与再次唤出；
- 深浅主题。

### M3：macOS 自用版

- 打包本地可运行版本；
- 在 Finder、浏览器、聊天和邮件场景测试；
- 连续自用一周；
- 只修真实摩擦，不扩功能。

### M4：Windows 适配

- Windows 构建；
- 资源管理器、浏览器、聊天、Office/邮件验收；
- DPI、多显示器和安装包验证。

## 12. 新项目的第一条任务

新项目创建后，第一条实现任务应直接使用以下描述：

> 创建 Flutter macOS 桌面工程，只实现跨应用文件拖放技术探针。使用 `super_drag_and_drop`：接收 Finder 拖入的单个和多个文件，在窗口内显示路径，并允许把同一文件从窗口拖回 Finder、浏览器上传区和一个真实聊天或邮件应用。不要创建 Windows 工程，不要做正式 UI、托盘、快捷键、持久化或产品功能。记录环境、依赖版本、成功路径、失败路径和是否需要原生补丁。只有探针通过后才能进入 MVP。

## 13. 当前未验证边界

- 已确定代码仓库地址为 `git@github.com:xiaomao361/dockshelf.git`，但尚未创建 Flutter 代码工程；
- 项目当前因开发机磁盘容量不足而暂停；
- 尚未运行 Flutter 技术探针；
- 尚未证明 macOS 跨应用拖出；
- 尚未证明 Windows 行为；
- 尚未测量安装包、内存和启动时间；
- 尚未决定开源、收费、签名、商店或官网分发；
- Flutter 是首选候选，不是未经验证的既成事实。
